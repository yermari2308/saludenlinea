name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping servidor
        run: curl -s https://saludenlinea.onrender.com/docs > /dev/null && echo "Servidor activo"
